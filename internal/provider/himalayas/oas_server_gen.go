// Code generated by ogen, DO NOT EDIT.

package himalayas

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// BrowseJobs implements browseJobs operation.
	//
	// Returns the full unfiltered remote jobs feed with offset/limit pagination. `limit` values above 20
	// are clamped to 20 server-side (no 400, despite the official document).
	//
	// GET /jobs/api
	BrowseJobs(ctx context.Context, params BrowseJobsParams) (*JobsResponse, error)
	// SearchJobs implements searchJobs operation.
	//
	// Returns filtered remote job results in the same JSON shape as `GET /jobs/api`, paginated by 1-based
	// `page` (fixed 20 per page; the response echoes the equivalent offset). A returned page may hold
	// fewer than 20 jobs even when more pages remain — paginate by totalCount, not page fullness.
	//
	// GET /jobs/api/search
	SearchJobs(ctx context.Context, params SearchJobsParams) (SearchJobsRes, error)
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
